# imports
import sys

# data import/export (fionn)
def parse_data(filename):
    #file = open(filename)



# slide area, etc (jack)
def slice_data(d):
    rows = d['rows']
    cols = d['cols']
    maxSlice = d['cells_per_slice']

    


# can be used (alex)

# scoring (bradley)

# RUN FILE

data = parse_data("")
